<!DOCTYPE html>
<html>
  <head>
    <title>IdeaFactory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <header>
      <nav>
      </nav>
      <nav class="navbar navbar-light bg-light">
        <div class="nav-container"> 
          <a class="text-primary navbar-brand" href="<%= root_path %>" style="margin-left: 1em">Idea Factory</a>
        </div>
        <div class="nav-items-set justify-content-end">
          <ul class="nav">
            <li class="nav-item">
              <a class="text-primary nav-link" href="<%= new_idea_path %>">New Idea</a>
            </li>


          </ul>
        </div>
      </nav>
    </header> 
    <div>
      <% flash.each do |type, message| %>
        <div class="alert alert-info" role="alert">
          <%= type + "------" + message %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
